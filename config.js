const config = {
    db: {
      /* don't expose password or any sensitive info, done only for demo */
      host: "db4free.net",
      user: "altezodb",
      password: "altezodb",
      database: "altezodb",
    },
    listPerPage: 10,
  };
  module.exports = config;